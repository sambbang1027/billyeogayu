<?xml version="1.0" encoding="UTF-8"?>
<!-- 작성자 : 이해든, 김민호, 서샘이, 황요한, 이원석 -->
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
    xmlns:task="http://www.springframework.org/schema/task"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd
        http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task.xsd
        http://mybatis.org/schema/mybatis-spring
        http://mybatis.org/schema/mybatis-spring.xsd">

    <!-- Component Scan - @Controller 제외 -->
    <context:component-scan base-package="app.domains">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
    <context:component-scan base-package="app.common">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
    
    <!-- SecurityConfig가 확실히 스캔되도록 명시적 추가 -->
	<context:component-scan base-package="app.common.config"/>	
    
    <!-- USERS -->
    <context:component-scan base-package="app.domains.users">
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <!-- Properties Configuration -->
    <context:property-placeholder location="classpath:application.properties" ignore-unresolvable="true"/>

    <!-- JavaMailSender Bean -->
    <bean id="javaMailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${spring.mail.host}"/>
        <property name="port" value="${spring.mail.port}"/>
        <property name="username" value="${spring.mail.username}"/>
        <property name="password" value="${spring.mail.password}"/>
        <property name="javaMailProperties">
            <props>
                <prop key="mail.transport.protocol">smtp</prop>
                <prop key="mail.smtp.auth">${spring.mail.smtp.auth}</prop>
                <prop key="mail.smtp.starttls.enable">${spring.mail.smtp.starttls.enable}</prop>
                <prop key="mail.smtp.starttls.required">${spring.mail.smtp.starttls.required}</prop>
                <prop key="mail.debug">true</prop>
                <prop key="mail.smtp.connectiontimeout">5000</prop>
                <prop key="mail.smtp.timeout">5000</prop>
                <prop key="mail.smtp.writetimeout">5000</prop>
            </props>
        </property>
    </bean>

    <!-- DataSource Configuration -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${db.driver}"/>
        <property name="jdbcUrl" value="${db.url}"/>
        <property name="username" value="${db.username}"/>
        <property name="password" value="${db.password}"/>
        <property name="maximumPoolSize" value="${db.maximumPoolSize:10}"/>
        <property name="minimumIdle" value="${db.minimumIdle:5}"/>
        <property name="connectionTimeout" value="${db.connectionTimeout:30000}"/>
        <property name="idleTimeout" value="${db.idleTimeout:600000}"/>
        <property name="maxLifetime" value="${db.maxLifetime:1800000}"/>
        <property name="leakDetectionThreshold" value="${db.leakDetectionThreshold:60000}"/>
    </bean>

    <!-- MyBatis SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis/mybatis-config.xml"/>
        <property name="mapperLocations" value="classpath*:mapper/**/*.xml"/>
        <!--  USERS -->
        <property name="typeAliasesPackage" value="app.domains"/>
    </bean>

    <!-- MyBatis Mapper Scan -->
 	<mybatis-spring:scan base-package="app.domains.*.dao"/>


    <!-- list용: Repository에서 SqlSessionTemplate 직접 사용 -->
    <bean id="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg index="0" ref="sqlSessionFactory"/>
    </bean>


    <!-- Transaction Manager -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- Transaction Annotation -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Task Scheduler Configuration -->
    <task:annotation-driven scheduler="alarmScheduler" executor="alarmExecutor"/>
    
    <!-- Scheduler Bean -->
    <bean id="alarmScheduler" class="org.springframework.scheduling.concurrent.ThreadPoolTaskScheduler">
        <property name="poolSize" value="5"/>
        <property name="threadNamePrefix" value="alarm-scheduler-"/>
        <property name="waitForTasksToCompleteOnShutdown" value="true"/>
        <property name="awaitTerminationSeconds" value="60"/>
    </bean>
    
    <!-- Executor Bean for Async Tasks -->
    <bean id="alarmExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="4"/>
        <property name="maxPoolSize" value="8"/>
        <property name="queueCapacity" value="100"/>
        <property name="threadNamePrefix" value="alarm-executor-"/>
        <property name="waitForTasksToCompleteOnShutdown" value="true"/>
        <property name="awaitTerminationSeconds" value="60"/>
    </bean>


</beans>